{
  "name": "sdlc-automator",
  "version": "2.0.0",
  "description": "Generic, project-agnostic SDLC automation pipeline using Claude Agent SDK",
  "type": "module",
  "scripts": {
    "start": "tsx src/run-tasks.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
